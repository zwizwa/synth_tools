%% The whole-system dataflow patch.  This spans multiple hosts.
%% Instead of building a new system, can it be built on top of redo?
%% Or do I use the epid patch language?  I don't think redo is a good
%% idea.  Let's keep that for building and restarting parts of the
%% patch.

-module(patch).
-export([]).




